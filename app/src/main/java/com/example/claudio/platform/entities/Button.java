package com.example.claudio.platform.entities;



/**
 * Created by Claudio on 31/05/2016.
 */
/*public class Button extends Entity {

    public Button(Loader loader, Vector3f position, int type, float[] vertices,
                  float[] textureCoords, int[] indices, int textureID){
        super();
        this.type = type;
        RawModel model = loader.loadToVAO(vertices, textureCoords, indices);
        ModelTexture texture = new ModelTexture(loader.loadTexture(textureID));
        texture.setNumberOfRows(1);
        TexturedModel texturedModel = new TexturedModel(model, texture);
        texturedModel.getTexture().setHasTransparency(true);
        setEntity(texturedModel, 0, position, dimension,
                new Vector3f(0,0,0) ,new Vector3f(1,1,1));
    }

}*/
